# NOTE: An example of kafka spec. Feel free to remove this file if not needed.
# RSpec.describe(Kafka::TestConsumer) do
#   it 'should consume a message' do
#     payload = {
#       'id' => 1,
#       'some_val' => 'hi mom'
#     }
#     expect(Rails.logger).to receive(:info).with("Payload is #{payload}")
#     test_consume_message(described_class, payload, call_original: true)
#   end
# end
